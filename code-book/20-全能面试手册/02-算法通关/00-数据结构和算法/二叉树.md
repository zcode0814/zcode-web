

## 遍历

> https://mp.weixin.qq.com/s/3MMgFtLW9BHpguUkHICLkQ

### 前中后序遍历

```java
class Solution {
    List<Integer> ans = new ArrayList<>();
    public List<Integer> preorderTraversal(TreeNode root) {
        if (root==null)return ans;
        ans.add(root.val);  // 前序
        preorderTraversal(root.left);
        preorderTraversal(root.right);
        return ans;
    }
}
```



![image-20220808225550847](https://yitiaoit.oss-cn-beijing.aliyuncs.com/img/image-20220808225550847.png)

### 层次遍历

```java
class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> ans = new ArrayList<>();
        if(root==null) return ans;
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        while(!queue.isEmpty()){
            List<Integer> item = new ArrayList<>();
            int n = queue.size();
            for (int i = 0; i < n; i++) {
                TreeNode node = queue.poll();
                item.add(node.val);
                if(node.left!=null){
                    queue.offer(node.left);
                }
                if(node.right!=null){
                    queue.offer(node.right);
                }
            }
            ans.add(item);
        }
        return ans;
    }
}
```

### 多叉树的遍历

> [590. N 叉树的后序遍历](https://leetcode.cn/problems/n-ary-tree-postorder-traversal/)
>
> [429. N 叉树的层序遍历](https://leetcode.cn/problems/n-ary-tree-level-order-traversal/)

对于二叉树来说，后序遍历的顺序为：递归左儿子（左子树）- 递归右儿子（右子树）- 根节点。

推广到 N 叉树，顺序是先依次递归其 `children` 数组中的节点（子树），再访问根节点。

```java
class Solution {
    List<Integer> list = new ArrayList();
    public List<Integer> postorder(Node root) {
        dfs(root);
        return list;
    }

    private void dfs(Node root){
        if(root==null){
            return;
        }
        for(Node child :  root.children){
            dfs(child);  
        }
        list.add(root.val);
    }
}
```

多叉树的层序遍历相比于二叉树，只需要将添加左右子树改为添加子节点数组。

```java
class Solution {
    public List<List<Integer>> levelOrder(Node root) {
        List<List<Integer>> ans = new ArrayList<>();
        if(root==null) return ans;
        Queue<Node> queue = new LinkedList<>();
        queue.offer(root);
        while(!queue.isEmpty()){
            int size = queue.size();
            List<Integer> item = new ArrayList<>(size);
            for (int i = 0; i < size; i++) {
                Node node = queue.poll();
                item.add(node.val);
                queue.addAll(node.children);
            }
            ans.add(item);
        }
        return ans;
    }
}
```











